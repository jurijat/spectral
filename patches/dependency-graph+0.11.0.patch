diff --git a/node_modules/dependency-graph/lib/dep_graph.js b/node_modules/dependency-graph/lib/dep_graph.js
--- a/node_modules/dependency-graph/lib/dep_graph.js
+++ b/node_modules/dependency-graph/lib/dep_graph.js
@@ -72,6 +72,7 @@ var DepGraph = (exports.DepGraph = function DepGraph(opts) {
   this.nodes = {}; // Node -> Node/Data (treated like a Set)
   this.outgoingEdges = {}; // Node -> [Dependency Node]
   this.incomingEdges = {}; // Node -> [Dependant Node]
+  this._outgoingEdgeSets = Object.create(null); // Node -> Set(Dependency Node)
   this.circular = opts && !!opts.circular; // Allows circular deps
 });
 DepGraph.prototype = {
@@ -94,6 +95,7 @@ DepGraph.prototype = {
       }
       this.outgoingEdges[node] = [];
       this.incomingEdges[node] = [];
+      this._outgoingEdgeSets[node] = new Set();
     }
   },
   /**
@@ -101,9 +103,13 @@ DepGraph.prototype = {
    */
   removeNode: function (node) {
     if (this.hasNode(node)) {
+      this.incomingEdges[node].forEach(function (from) {
+        this._outgoingEdgeSets[from].delete(node);
+      }, this);
       delete this.nodes[node];
       delete this.outgoingEdges[node];
       delete this.incomingEdges[node];
+      delete this._outgoingEdgeSets[node];
       [this.incomingEdges, this.outgoingEdges].forEach(function (edgeList) {
         Object.keys(edgeList).forEach(function (key) {
           var idx = edgeList[key].indexOf(node);
@@ -151,10 +157,9 @@ DepGraph.prototype = {
     if (!this.hasNode(to)) {
       throw new Error("Node does not exist: " + to);
     }
-    if (this.outgoingEdges[from].indexOf(to) === -1) {
+    if (!this._outgoingEdgeSets[from].has(to)) {
+      this._outgoingEdgeSets[from].add(to);
       this.outgoingEdges[from].push(to);
-    }
-    if (this.incomingEdges[to].indexOf(from) === -1) {
       this.incomingEdges[to].push(from);
     }
     return true;
@@ -165,6 +170,7 @@ DepGraph.prototype = {
   removeDependency: function (from, to) {
     var idx;
     if (this.hasNode(from)) {
+      this._outgoingEdgeSets[from].delete(to);
       idx = this.outgoingEdges[from].indexOf(to);
       if (idx >= 0) {
         this.outgoingEdges[from].splice(idx, 1);
@@ -190,6 +196,7 @@ DepGraph.prototype = {
       result.nodes[n] = source.nodes[n];
       result.outgoingEdges[n] = source.outgoingEdges[n].slice(0);
       result.incomingEdges[n] = source.incomingEdges[n].slice(0);
+      result._outgoingEdgeSets[n] = new Set(source._outgoingEdgeSets[n]);
     });
     return result;
   },
@@ -313,9 +320,10 @@ DepGraph.prototype = {
       // nodes that did not end up in the initial result (as they are part of a
       // subgraph that does not have a clear starting point)
       if (this.circular) {
+        var _seen = new Set(result);
         keys
           .filter(function (node) {
-            return result.indexOf(node) === -1;
+            return !_seen.has(node);
           })
           .forEach(function (n) {
             DFS(n);
